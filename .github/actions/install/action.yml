name: Install Dependencies
runs:
  using: composite
  steps:
    - name: Install pnpm
      uses: pnpm/action-setup@v4
      with:
        version: 8

    - run: pnpm install --frozen-lockfile
      shell: bash
